# Repository media

Put MP4, PNG, JPG, GLB, GLTF, PDF, and text files in this folder, then commit and push. The build automatically finds every file here and puts it on the shelf. You do not need to edit `src/main.ts`.

Large videos may be better stored with Git LFS. Browser uploads are temporary local previews and do not write back to GitHub automatically.
